+++
title = "Projects — pokédex"
template = "projects-dex.html"

[extra]
view = "/projects-dex"
+++

One repo, one card. The primary type comes from what the project does, the
secondary type from its language, the HP from its star count, the attacks from its
own topics, the level from its age. Nothing is invented. The sprite is computed
from the repo name, so it will never change.
